file-name-convention:
  ignore: []

  behavior_pack:
    animation_controllers: '.animation_controllers.json'
    animations: '.animation.json'
    biomes: '.biome.json'
    blocks: '.block.json'
    dialogue: '.dialogue.json'
    entities: '.behavior.json'
    features: '.feature.json'
    feature_rules: '.feature_rule.json'
    items: '.item.json'
    loot_tables: '.loot_table.json'
    recipes: '.recipe.json'
    spawn_rules: '.spawn_rule.json'
    trading: '.trade.json'

  resource_pack:
    animation_controllers: '.animation_controllers.json'
    animations: '.animation.json'
    attachables: '.attachable.json'
    entity: '.entity.json'
    fog: '.fog_setting.json'
    models: '.geo.json'
    items: '.item.json'
    particles: '.particle.json'
    render_controllers: '.render_controllers.json'

name-patterns:
  ignore: []

  identifiers: '^(?:(?:(?!minecraft|minecon))+[a-z_]{3,}:[a-z_]{3,})$'
  animation_controllers: '^(?:controller.animation.)+[a-z0-9_.]+$'
  animations: '^(?:animation.)+[a-z0-9_.]+$'
  geometries: '^(?:geometry.)+[a-z_]+$'
