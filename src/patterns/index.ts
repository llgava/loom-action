import path from 'path';

const custom = './pattern.yml';
const mojang = path.join(__dirname, 'patterns', 'mojang.yml');
const seface_blocks = path.join(__dirname, 'patterns', 'seface_blocks.yml');

export { custom, mojang, seface_blocks };
